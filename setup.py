from setuptools import setup, find_packages

setup(
    name='helper_functions',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Lista de suas dependências aqui
    ],
)